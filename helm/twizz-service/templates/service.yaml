apiVersion: v1
kind: Service
metadata:
  name: {{ include "twizz-service.fullname" . }}
  labels:
    {{- include "twizz-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "twizz-service.selectorLabels" . | nindent 4 }}
