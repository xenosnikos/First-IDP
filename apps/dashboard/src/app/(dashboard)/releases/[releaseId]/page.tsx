export default function ReleaseDetailPage({ params }: { params: Promise<{ releaseId: string }> }) {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-6">Release Detail</h1>
      {/* TODO: Release info, check results, promote button, docs link */}
    </div>
  );
}
